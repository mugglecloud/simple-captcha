<!DOCTYPE html>
<html lang="en">
  <head>
    <title>人机识别验证码</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <style>
      html,
      body {
        width: 100vw;
        height: 100vh;
        margin: 0;
        padding: 0;
        font-size: 16px;
      }

      #root {
        display: flex;
        width: 100%;
        height: 50%;
        justify-content: center;
        align-items: center;
      }

      #root form {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        width: 200px;
      }

      form > * {
        margin-bottom: 10px;
        width: 100%;
        box-sizing: border-box;
      }

      form > input {
        padding: 0.5em;
        height: 32px;
      }

      form > button {
        font-size: 16px;
        padding: 5px 10px;
      }
    </style>
  </head>
  <body>
    <div id="root">
      <form>
        <img src="data:image/png;base64,{{ captcha }}"/>
        <input name="captcha" placeholder="请输入验证码"/>
        <input name="client_ip" hidden value="{{client_ip}}"/>
        <button type="submit">我不是机器</button>
      </form>
    <div>
  </body>
</html>
